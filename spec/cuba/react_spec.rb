require 'spec_helper'

describe Cuba::React do
  it 'has a version number' do
    expect(Cuba::React::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
